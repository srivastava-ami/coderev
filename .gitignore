# compiled binaries
bin/
/main

# generated scan report (artifact, never committed — view in Actions step summary or as artifact)
coderev-report.md

# knowledge graph (auto-rebuilt by post-commit hook)
graphify-out/

# agent / dev-environment files (not part of the deliverable)
.claude/
.mcp.json
project_manifest.toml
CLAUDE.md
AGENTS.md

# temporary logs from Claude tool executions
.tmp/

# implementation plan (dev-only, never shipped)
plan.md

# dispatch prompt and temporary logs
.dispatch-prompt.txt
.tmp-scan.log

# compiled binary (always build fresh via go build)
/coderev

# OS
.DS_Store

# opencode config
opencode.json

# coderev generated scan artifacts — regenerate on every run
.coderev/

# temporary work documents (phase summaries, spec drafts, snippet collections)
COPY_PASTE_SNIPPETS.md
PHASE2_SUMMARY.txt
TERRAFORM_PHASE1_SPECIFICATION.md

# git worktrees (isolated development, not part of main branch)
.worktrees/

# git worktrees (isolated development, not part of main branch)
.worktrees/
